sudo: required

language: cpp

compiler:
  - g++

services:
  - docker

env:
  - distro: ubuntu1604

before_install:
  - docker run -it ubuntu bash

install:
 - add-apt-repository -y ppa:ubuntu-toolchain-r/test 
 - apt-get update
 - apt-get install gcc-17 
 - apt-get install g++-17 

script: "bash -c ./scripts/travis/ubuntu-16-build.sh"
 
